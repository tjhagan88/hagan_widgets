<% @page_title = "Delete User: #{@user.name}" %>

<%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>

<div class="user delete">
  <h2>Delete User</h2>

  <%= form_for(:user, :url => {:action => 'destroy', :user_id => @user.id}) do |f| -%>

    <p>Are you sure you want to permanently delete this admin user?</p>

    <p class="reference-name"><%= @user.name %></p>

    <div class="form-buttons">
      <%= submit_tag("Delete User") %>
    </div>

  <% end %>
</div>
